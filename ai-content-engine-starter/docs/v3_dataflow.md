# V3 Dataflow

source
→ collector
→ raw_items
→ normalization
→ deduplication
→ scoring
→ editorial planner
→ publish intent
→ asset generation
→ policy evaluation
→ scheduler
→ publisher
→ telegram channel

Feedback loop:
published post → analytics → ranking → editorial planner
